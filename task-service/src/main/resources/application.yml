server:
  port: 8080

spring:
  application:
    name: task-service

notification:
  service:
    base-url: ${NOTIFICATION_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info